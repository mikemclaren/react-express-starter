var React = require('react'), ToDoListicle;

ToDoListicle = React.createClass(
  {
    render: function render() {
      return (
        <div>Potato.</div>
      );
    }
  }
);

module.exports = ToDoListicle;
