var React = require('react'), ToDoListForm;

ToDoListForm = React.createClass(
  {
    render: function render() {
      return (
        <div>Potato.</div>
      );
    }
  }
);

module.exports = ToDoListForm;
